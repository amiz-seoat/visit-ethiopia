### Frontend Vite env example
# Copy to `.env` or use local env handling for Vite
# Make sure this points to your backend's users base path if frontend API calls assume /users in base URL
VITE_BACKEND_URL=http://localhost:3000/api/v1/users
